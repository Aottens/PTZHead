---
phase: 03-feedback-and-discovery
task: null
total_tasks: null
status: paused
last_updated: 2026-04-05T09:02:53.317Z
---

<current_state>
Mid-`/gsd:discuss-phase 3`. Prior context fully loaded (PROJECT.md, REQUIREMENTS.md,
ROADMAP.md, Phase 2 CONTEXT.md) and codebase scouted (src/ptz_osc.*, src/ptz_config.h,
src/main.cpp, src/ptz_wifi.*). Gray areas were analyzed and presented to the user,
but the user paused before answering. No CONTEXT.md written yet. No code touched.
</current_state>

<completed_work>

- Loaded project/requirements/state + Phase 2 CONTEXT.md
- Scouted existing code (ptz_osc module, config, main loop, wifi module)
- Analyzed Phase 3 domain and identified 4 phase-specific gray areas
- Presented gray areas to user with carry-forward decisions annotated
- Created .planning/phases/03-feedback-and-discovery/ directory (was missing)
</completed_work>

<remaining_work>

- User picks which gray areas to discuss (default: all 4)
- Deep-dive each selected area (4 questions per area)
- Write 03-CONTEXT.md with decisions + canonical_refs + code_context
- Commit docs + STATE.md update
- Then: /gsd:plan-phase 3 → /gsd:execute-phase 3
</remaining_work>

<decisions_made>

- Phase 3 scope = FB-01..FB-04 (OSC status feedback) + NET-03 (mDNS) — matches roadmap
- Carry forward from Phase 2: CNMAT/OSC lib, UDP port 8000, integer-only feedback, single Companion sender assumption, 500ms heartbeat untouched
</decisions_made>

<blockers>
- Waiting on user answer to "which gray areas to discuss?" prompt
</blockers>

<context>
The four gray areas surfaced:

1. **Feedback address schema** — bundled status vs separate scalar addresses per field
   (Companion generic-osc prefers one scalar per address — leaning separate)
2. **Send trigger/cadence** — on-change, periodic, or mixed (e.g. moving flags on-change,
   RSSI every 1–2s)
3. **Reply target** — reply-to-sender (cache IP/port from last RX packet in PtzOsc) vs
   fixed IP vs broadcast. Phase 2 CONTEXT.md already earmarked reply-to-sender for Phase 3.
4. **mDNS service details** — hostname `ptzhead.local` locked; open questions: TXT records,
   service instance name, begin-on-connect vs begin-on-reconnect

Key constraints to remember:
- FB-04 forces integer values on wire (Companion generic-osc compatibility)
- Heartbeat watchdog must NOT be confused by outgoing feedback traffic — lastRxMs_ tracks
  RX only; stays independent
- PtzOsc already owns WiFiUDP (s_udp) — extend it to send replies, don't add a second UDP
- mDNS likely lives in PtzWifi (or new small helper) and needs to re-advertise on reconnect
</context>

<next_action>
Re-enter the discuss-phase flow by running `/gsd:discuss-phase 3` (or continuing conversation).
Ask the user which of the 4 gray areas to discuss — default "all" — then ask 4 questions
per selected area with code-informed options.
</next_action>
